# 2026-05-18

## Claude Code 再次修复 + 根因解决

### 问题重现
- 昨晚让Claude Code清理C盘，今天Claude Code又打不开了
- 原因：Claude Code清C盘时删掉了自己的npm全局包（`AppData\Roaming\npm\node_modules\@anthropic-ai\claude-code`）
- 只剩1个文件，其余全部被删

### 一劳永逸方案
- 将npm全局安装路径从C盘迁到D盘：`npm config set prefix "D:\npm-global"`
- 重装Claude Code到D盘
- 更新PATH环境变量
- 清理C盘旧npm残留
- **以后Claude Code清C盘不会再删到自己**

### C盘垃圾清理
- 扫描+清理，释放约3GB
- 绿标区（安全）：用户Temp 937MB、浏览器缓存 ~420MB、npm/pip缓存 ~400MB、旧npm 219MB、系统日志 36MB
- 黄标区（确认后删）：Chrome更新缓存 458MB、VS Code/WorkBuddy安装包残留 ~980MB
- Discord（462MB）和Midjourney（542MB）按用户要求保留
- 桌面26GB是大头，未动，用户自行整理

### 剩余
- C盘：120G总 → 108G已用 → 13G剩余（从10G改善到13G）
- 桌面26GB如果整理能再腾出大片空间

### D盘垃圾清理
- D盘：117G总 → 清理前108G已用/8.6G剩余(93%)，清理后98G已用/19G剩余(85%)
- 释放约10GB
- 爱奇艺PC客户端：发现10个旧版本累积(~8.3GB)，只保留最新14.2.5.9826
- 删除：360安全浏览器下载(897MB)、YOUKU+Youku Files(846MB)、5个空下载文件夹
- 保留：陈忠材(52GB个人资料)、直播回放(4.6GB)、所有程序文件

### D盘根目录整理
- 根目录原本散落100+个文件（dll、exe、配置、压缩包），极度混乱
- 删除：iTunes全套(42文件+11文件夹,~300MB)、ManicTime全套(16文件)、FaceRig残留下载(2GB)、旧压缩包(220MB)
- 归类创建4个文件夹：`_WinRAR\`(24文件)、`_旧程序残留\`(82个dll)、`_安装包\`(3个exe)、`_杂项\`(19个配置)
- 根目录从100+散落文件缩减到7个（pagefile.sys系统文件+cc-switch等少量必需文件）

### Remotion 视频音频集成
- 基于Claude Code已搭建的秦始皇动画（6场景/780帧），独立复制项目到 `C:\Users\miaoh\WorkBuddy\Claw\remotion-project\`
- 音频生成脚本 `scripts/gen_audio.py`：edge-tts旁白（6段，zh-CN-YunjianNeural男声）+ 正弦波合成背景音乐 + 4个音效
- 编辑 `src/Composition.tsx`：添加 Audio/staticFile 导入，6个Sequence各带旁白，顶层背景音乐，场景4添加轰鸣音效，场景5添加冲击音效
- 编译零错误，渲染成功（780帧，1.7MB MP4）
- Claude Code的原始项目 `D:\remotion-project\` 未被修改