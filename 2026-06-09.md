# 2026-06-09 工作日志

## Claude Code 修复（最终方案）

### 问题
Claude Code 无法连接 API，cc-switch 代理将请求转发到 `https://api.deepseek.com/anthropic`，但该端点返回 404。

### 根因
1. cc-switch 配置了 `apiFormat: anthropic`，直接将 Anthropic 格式请求透传到 DeepSeek 的 `/anthropic` 端点
2. DeepSeek 的 `/anthropic` 端点实际返回 404（尽管文档说支持，但实测不可用）
3. cc-switch 代理模式不稳定：启动后几秒就自动停止并恢复 settings.json 备份

### 解决方案
绕过 cc-switch，自建本地 Anthropic-to-OpenAI 转换代理：
- **代理脚本**: `C:\Users\miaoh\anthropic-proxy\proxy.py`
- **监听端口**: 127.0.0.1:15721（与 cc-switch 同端口，确保 settings.json 不需要改动）
- **功能**: 将 Claude Code 的 Anthropic Messages API 请求转换为 OpenAI Chat Completions 格式，转发给 DeepSeek API
- **启动方式**: Windows 启动文件夹 `start-proxy.vbs`
- **settings.json**: `ANTHROPIC_BASE_URL` 设为 `http://127.0.0.1:15721`

### 关键修改
1. 停止 cc-switch（`taskkill /F /IM cc-switch.exe`）
2. 修改 `~/.claude/settings.json` 中 `ANTHROPIC_BASE_URL` 从 `https://api.deepseek.com/anthropic` 改为 `http://127.0.0.1:15721`
3. 部署 `C:\Users\miaoh\anthropic-proxy\proxy.py`（FastAPI + uvicorn）
4. 将 `start-proxy.vbs` 放入 Windows 启动文件夹

### 验证
- `claude -p "say hi"` → 成功返回
- `claude -p "1+1等于几？"` → 成功返回

### cc-switch 数据库修改（已做但可能被覆盖）
- providers 表 DeepSeek provider: `ANTHROPIC_BASE_URL` 改为 `https://api.deepseek.com/v1`，`apiFormat` 改为 `openai_chat`
- provider_endpoints 表: URL 改为 `https://api.deepseek.com/v1`
- 注意：cc-switch 重启后可能会覆盖这些修改
