# 2026-05-24 日志

## Claude Code 代理问题（再次复发）
- YZ-VPN 的 sing-box 重启后把 config.json 里的 set_system_proxy 改回了 true
- 再次修改 D:\VPN\YZ-VPN\resources\bin\win32-ia32\config.json，将 set_system_proxy 改为 false
- 清除 Windows 系统代理注册表 ProxyEnable=0, ProxyServer=""
- 建议用户把 config.json 设为只读（attrib +R）防止 VPN 再次覆盖
- 根本原因：YZ-VPN 每次重启会重置 config.json，需要一劳永逸的只读方案

## Claude Code 红字报错修复（14:25）
- 错误：`claude.exe` 无法运行，"不是此操作系统平台的有效应用程序"
- 原因：安装时 optional dependencies 未下载，`bin/claude.exe` 是个 500 字节的占位 shell 脚本
- 修复步骤：
  1. 手动安装平台包：`npm install --prefix D:/npm-global @anthropic-ai/claude-code-win32-x64@2.1.150`
  2. 重装主包：`npm install --prefix D:/npm-global @anthropic-ai/claude-code@2.1.150`
  3. 运行 postinstall：`node D:/npm-global/node_modules/@anthropic-ai/claude-code/install.cjs`
- 修复后：`claude.exe` 大小 234MB（真正的 Windows PE 可执行文件），`claude --version` 正常输出 2.1.150

## Claude Code 红字报错再次修复（14:40）
- 用户用腾讯电脑管家清理 D 盘，234MB 的 claude.exe 和 claude-code-win32-x64 包被当垃圾删除
- claude.exe 被还原回 500 字节占位脚本
- 修复：重新安装平台包 + 运行 postinstall
- **防复发措施**：用 `attrib +R +S` 将 claude-code-win32-x64 目录设为只读+系统属性，`claude.exe` 设为只读
- ⚠️ 如果未来要升级 claude-code，需先 `attrib -R -S` 去掉只读属性才能替换文件
