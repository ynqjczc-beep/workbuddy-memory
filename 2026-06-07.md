# 2026-06-07 日记

## Claude Code 修复——claude.exe 被替换成 Bun 运行时

### 问题现象
用户在 PowerShell 运行 `claude`，显示的是 Bun v1.3.14 的帮助信息，而不是 Claude Code。

### 根本原因
`D:\npm-global\node_modules\@anthropic-ai\claude-code\bin\claude.exe` 被替换成了 Bun 运行时（240MB，MZ 头但内容是 Bun）。真正的 Claude Code 可执行文件被重命名存放在：
`D:\npm-global\node_modules\@anthropic-ai\claude-code-win32-x64\claude.exe.old.1780309607003`（234MB，真正的 Claude Code v2.1.150）

原因：postinstall 脚本在更新时未正确执行，导致平台包里的 claude.exe 被标记为 .old 但没有完成替换。

### 修复步骤
1. 备份错误的 Bun claude.exe → `claude.exe.bak.bun`
2. 把真正的 `claude.exe.old.1780309607003` 复制到 `bin/claude.exe`
3. 验证：`claude --version` 输出 `2.1.150 (Claude Code)` ✅

### 注意
- 当前版本是 2.1.150，包版本声明是 2.1.167，有版本差异（平台包未同步更新）
- 配置文件 `~/.claude/settings.json` 指向 DeepSeek API（api.deepseek.com/anthropic），API 连通正常
- 如果以后升级 claude-code，需要确保 postinstall 正确执行，否则会再次出现此问题
